---
phase: 01-sanity-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - sanity/schemaTypes/documents/category.ts
  - sanity/schemaTypes/documents/galleryItem.ts
  - sanity/schemaTypes/index.ts
  - sanity/structure.ts
  - sanity.config.ts
autonomous: true
requirements:
  - SAN-01
  - SAN-02
must_haves:
  truths:
    - "Editors can manage category documents with title, slug, description, cover image, and display order."
    - "Editors can manage gallery item documents with one category reference, visibility, and home page curation fields."
    - "The Studio enforces the approved information architecture: three category slots and no multi-category gallery items."
  artifacts:
    - path: "sanity/schemaTypes/documents/category.ts"
      provides: "Category document schema"
    - path: "sanity/schemaTypes/documents/galleryItem.ts"
      provides: "Gallery item document schema"
    - path: "sanity/structure.ts"
      provides: "Studio structure and category governance rules"
  key_links:
    - from: "sanity/schemaTypes/documents/galleryItem.ts"
      to: "sanity/schemaTypes/documents/category.ts"
      via: "strong reference field"
      pattern: "type: \"reference\""
    - from: "sanity.config.ts"
      to: "sanity/structure.ts"
      via: "desk tool structure registration"
      pattern: "structureTool"
---

<objective>
Define the Sanity content model and Studio behavior for Work categories and gallery items.

Purpose: Encode the user’s approved information architecture directly into schema validation and Studio structure before any content migration begins.
Output: Registered `category` and `galleryItem` document schemas plus Studio structure rules that keep category management constrained to the approved three-slot model.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sanity-foundation/01-CONTEXT.md
@.planning/phases/01-sanity-foundation/01-RESEARCH.md
@.planning/phases/01-sanity-foundation/01-VALIDATION.md
@sanity.config.ts
@app/types/index.ts
@lib/api.ts
@public/admin/config.yml

<interfaces>
From app/types/index.ts:
```ts
export interface ProjectImage {
  src: string;
  alt: string;
  width?: number;
  height?: number;
}

export interface Project {
  id: string;
  slug: string;
  title: string;
  categories: string[];
  coverImage: string;
  coverAlt?: string;
  images: ProjectImage[];
  isFeatured: boolean;
  isHidden?: boolean;
}
```

From lib/api.ts:
```ts
export function getProjects(): Project[];
export function getProjectBySlug(slug: string): Project | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the category schema with exact editorial fields</name>
  <files>sanity/schemaTypes/documents/category.ts, sanity/schemaTypes/index.ts</files>
  <read_first>sanity.config.ts, app/types/index.ts, public/admin/config.yml, .planning/phases/01-sanity-foundation/01-CONTEXT.md</read_first>
  <action>Create `sanity/schemaTypes/documents/category.ts` using `defineType` and `defineField` for `name: "category"` with fields `title` (`string`, required), `slug` (`slug`, required, source `title`, maxLength 96), `description` (`text`, required), `coverImage` (`image`, required, options `{ hotspot: true }`, nested `alt` string field required), and `displayOrder` (`number`, required, integer, min 1, max 3). Add schema preview using `title` and `coverImage`. Register `category` in `sanity/schemaTypes/index.ts`. This task implements D-01, D-02, and D-03 by keeping the category model limited to the three canonical slots and not carrying forward the legacy five-category list.</action>
  <verify>
    <automated>npm run lint -- sanity/schemaTypes/documents/category.ts sanity/schemaTypes/index.ts</automated>
  </verify>
  <acceptance_criteria>`sanity/schemaTypes/documents/category.ts` exists; `rg -n 'name: \"category\"|displayOrder|max\\(3\\)|coverImage|description' sanity/schemaTypes/documents/category.ts` returns matches; `sanity/schemaTypes/index.ts` exports `category` in the schema array.</acceptance_criteria>
  <done>The category schema exposes the exact fields required by SAN-01 and uses validation consistent with a three-category system.</done>
</task>

<task type="auto">
  <name>Task 2: Define the gallery item schema with a single strong category reference</name>
  <files>sanity/schemaTypes/documents/galleryItem.ts, sanity/schemaTypes/index.ts</files>
  <read_first>sanity/schemaTypes/documents/category.ts, sanity/schemaTypes/index.ts, app/types/index.ts, .planning/phases/01-sanity-foundation/01-CONTEXT.md</read_first>
  <action>Create `sanity/schemaTypes/documents/galleryItem.ts` using `defineType` and `defineField` for `name: "galleryItem"` with fields `title` (`string`, optional editorial label), `image` (`image`, required, options `{ hotspot: true }`), `alt` (`string`, required), `category` (`reference`, required, `to: [{ type: "category" }]`, strong reference only, not an array), `isVisible` (`boolean`, required, initial value `true`), `showOnHomePage` (`boolean`, required, initial value `false`), and `homePageOrder` (`number`, hidden unless `showOnHomePage === true`, validation allowing blank when hidden and min 1 when shown). Register `galleryItem` in `sanity/schemaTypes/index.ts`. This task implements D-04 through D-09 by making each image its own document, keeping one category per image, and using a boolean plus explicit order for home curation.</action>
  <verify>
    <automated>npm run lint -- sanity/schemaTypes/documents/galleryItem.ts sanity/schemaTypes/index.ts</automated>
  </verify>
  <acceptance_criteria>`sanity/schemaTypes/documents/galleryItem.ts` exists; `rg -n 'name: \"galleryItem\"|name: \"category\"|type: \"reference\"|showOnHomePage|homePageOrder|isVisible' sanity/schemaTypes/documents/galleryItem.ts` returns matches; the file contains no `type: \"array\"` reference field for categories.</acceptance_criteria>
  <done>The gallery item schema encodes the approved single-image, single-category, simple-home-curation model from the phase decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Register Studio structure that exposes only the approved category workflow</name>
  <files>sanity/structure.ts, sanity.config.ts</files>
  <read_first>sanity.config.ts, sanity/schemaTypes/documents/category.ts, sanity/schemaTypes/documents/galleryItem.ts, .planning/phases/01-sanity-foundation/01-RESEARCH.md, .planning/phases/01-sanity-foundation/01-CONTEXT.md</read_first>
  <action>Create `sanity/structure.ts` with a desk structure that shows two top-level sections: `Work Categories` and `Gallery Items`. In `Work Categories`, present three fixed creation templates or slots tied to `displayOrder` values `1`, `2`, and `3`, and remove the generic extra-category creation path so the Studio does not encourage a fourth category. In `sanity.config.ts`, add `structureTool({ structure })` to `plugins` alongside the default desk tooling and keep `schema: { types: schemaTypes }`. The action text for this plan must implement D-01 and D-03 fully: no hidden extra categories, no legacy five-category carryover, and no open-ended create menu for more categories.</action>
  <verify>
    <automated>npm run lint -- sanity/structure.ts sanity.config.ts</automated>
  </verify>
  <acceptance_criteria>`sanity/structure.ts` exists; `rg -n 'Work Categories|Gallery Items|displayOrder|template|category' sanity/structure.ts` returns matches; `sanity.config.ts` contains `structureTool` in `plugins`; no task action or file content references the deferred multi-category model.</acceptance_criteria>
  <done>The Studio surface guides editors into exactly three category slots and registered document types consistent with SAN-01 and SAN-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio editor→schema validation | Untrusted editorial input crosses into persisted CMS documents |
| `galleryItem`→`category` reference | Content integrity depends on a valid category target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `sanity/schemaTypes/documents/galleryItem.ts` | mitigate | Require a single strong `category` reference and do not use arrays or weak refs, preventing unauthorized cross-category drift and broken joins. |
| T-01-05 | I | `sanity/schemaTypes/documents/category.ts` | mitigate | Require `alt` text on the category cover image and required editorial fields so incomplete documents do not surface opaque or missing media metadata later. |
| T-01-06 | E | `sanity/structure.ts` | mitigate | Remove the generic extra-category creation path and expose only the three approved category slots so editors cannot escalate the IA beyond D-01/D-03 through Studio UI shortcuts. |
</threat_model>

<verification>
Run `npm run lint` after each task and `npm run build` after Task 3. Confirm `rg -n 'category|galleryItem|showOnHomePage|homePageOrder|structureTool' sanity sanity.config.ts` returns the schema and structure registrations.
</verification>

<success_criteria>
The Studio registers both Work document types, gallery items cannot be assigned to multiple categories, and the category workflow is constrained to the approved three-category information architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sanity-foundation/01-02-SUMMARY.md`
</output>
