---
phase: 01-sanity-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .env.example
  - README.md
autonomous: false
requirements:
  - SAN-01
  - SAN-02
must_haves:
  truths:
    - "A developer can see which Sanity environment variables are required before opening the Studio."
    - "Editors have a concrete verification checklist for local `/studio` access, login, CORS, and category/gallery document creation."
    - "The setup docs explicitly state the Phase 1 Sanity access model: two Administrator users on the Free plan, with no extra role or permission modeling."
    - "The repo’s documented setup matches the embedded Studio and schema files created in this phase."
  artifacts:
    - path: ".env.example"
      provides: "Required public Sanity environment variable names"
    - path: "README.md"
      provides: "Local setup and manual verification steps for embedded Studio"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "documented env setup steps"
      pattern: "NEXT_PUBLIC_SANITY_PROJECT_ID"
    - from: "README.md"
      to: "app/studio/[[...tool]]/page.tsx"
      via: "manual verification route instructions"
      pattern: "/studio"
    - from: "README.md"
      to: ".planning/phases/01-sanity-foundation/01-CONTEXT.md"
      via: "documented Phase 1 access-model assumptions"
      pattern: "Administrator|Free plan|custom roles"
---

<objective>
Document the Sanity setup and close Phase 1 with verifiable Studio access instructions.

Purpose: Ensure the repo is usable by humans after the code foundation lands, including the external CORS/auth steps automation cannot complete.
Output: Environment variable template, Sanity setup documentation, and a blocking human verification checkpoint for `/studio`.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-sanity-foundation/01-CONTEXT.md
@.planning/phases/01-sanity-foundation/01-RESEARCH.md
@.planning/phases/01-sanity-foundation/01-VALIDATION.md
@README.md
@app/studio/[[...tool]]/page.tsx
@sanity.config.ts
@sanity/env.ts
@sanity/schemaTypes/documents/category.ts
@sanity/schemaTypes/documents/galleryItem.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Sanity environment template</name>
  <files>.env.example</files>
  <read_first>.env.example, sanity/env.ts, .planning/phases/01-sanity-foundation/01-RESEARCH.md</read_first>
  <action>Create or update `.env.example` to include `NEXT_PUBLIC_SANITY_PROJECT_ID=`, `NEXT_PUBLIC_SANITY_DATASET=production`, and `NEXT_PUBLIC_SANITY_API_VERSION=2026-03-01`. Add a short comment that these are public Sanity identifiers used by the embedded Studio and client, and do not include any write token or secret in tracked env examples.</action>
  <verify>
    <automated>npm run lint -- .env.example sanity/env.ts</automated>
  </verify>
  <acceptance_criteria>`.env.example` exists; `rg -n 'NEXT_PUBLIC_SANITY_PROJECT_ID|NEXT_PUBLIC_SANITY_DATASET|NEXT_PUBLIC_SANITY_API_VERSION' .env.example` returns matches; `.env.example` contains no token variable such as `SANITY_API_TOKEN`.</acceptance_criteria>
  <done>The repo exposes the exact Sanity env keys needed for local setup without risking secret leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Document Studio setup, CORS, and schema verification workflow</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/01-sanity-foundation/01-VALIDATION.md, sanity.config.ts, sanity/schemaTypes/documents/category.ts, sanity/schemaTypes/documents/galleryItem.ts</read_first>
  <action>Update `README.md` with a `Sanity Studio setup` section that lists install/setup steps, `.env.example` variable meanings, the `/studio` route, and the Sanity Manage requirement to add local and deployed origins to CORS with authenticated requests enabled. Add a short `Sanity access model` subsection that explicitly documents D-10 and D-11: Phase 1 assumes two Administrator users on Sanity's Free plan for Thalita and her collaborator, and custom roles or extra permission modeling are out of scope in this phase. Add a manual verification checklist that explicitly covers: running `npm run dev`, opening `/studio`, authenticating, confirming `Work Categories` and `Gallery Items` appear, creating or editing up to three category docs with `title`, `slug`, `description`, `coverImage`, and `displayOrder`, and creating a `galleryItem` with `image`, `alt`, single `category`, `isVisible`, `showOnHomePage`, and `homePageOrder` when shown.</action>
  <verify>
    <automated>rg -n 'Sanity Studio setup|Sanity access model|Administrator|Free plan|custom roles|/studio|Work Categories|Gallery Items|NEXT_PUBLIC_SANITY_PROJECT_ID|showOnHomePage' README.md</automated>
  </verify>
  <acceptance_criteria>`README.md` contains the heading `Sanity Studio setup`; `rg -n 'CORS|/studio|Work Categories|Gallery Items|NEXT_PUBLIC_SANITY_PROJECT_ID|showOnHomePage|Administrator|Free plan|custom roles' README.md` returns matches; the checklist mentions exactly three categories and a single category reference for each gallery item; the docs state that no extra role modeling is part of Phase 1.</acceptance_criteria>
  <done>The repo documents the full human setup and validation workflow needed to use the Phase 1 Sanity foundation safely.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify embedded Studio access and schema behavior</name>
  <files>.env.local, app/studio/[[...tool]]/page.tsx, sanity/schemaTypes/documents/category.ts, sanity/schemaTypes/documents/galleryItem.ts</files>
  <read_first>README.md, .planning/phases/01-sanity-foundation/01-VALIDATION.md, app/studio/[[...tool]]/page.tsx, sanity/schemaTypes/documents/category.ts, sanity/schemaTypes/documents/galleryItem.ts</read_first>
  <action>Execute the README verification flow against the real embedded Studio route after Tasks 1 and 2 are complete. Treat this as a blocking confirmation step for SAN-01 and SAN-02: the goal is to prove the repo-side setup, category workflow, and gallery-item workflow behave as documented before Phase 1 is considered complete.</action>
  <what-built>Embedded Studio route, category and gallery schemas, environment template, and setup documentation for Phase 1.</what-built>
  <how-to-verify>1. Copy `.env.example` values into `.env.local` with the real Sanity project ID and dataset. 2. Run `npm run dev`. 3. Open `http://localhost:3000/studio`. 4. Confirm the Studio shell loads and authentication succeeds. 5. Confirm the top-level navigation shows `Work Categories` and `Gallery Items`. 6. Review the README `Sanity access model` notes and confirm the current Phase 1 setup assumes two Administrator users on the Free plan, with no custom roles or permission configuration required in the repo. 7. Create or inspect category documents and confirm no workflow encourages more than three category slots. 8. Create a gallery item and confirm only one `category` field is available, `showOnHomePage` is boolean, and `homePageOrder` appears only when that toggle is enabled.</how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>`npm run build` passes before the checkpoint; the user confirms `/studio` loads, the two document types appear, category editing is limited to the approved three-slot workflow, and gallery items expose a single category field plus the home curation controls.</acceptance_criteria>
  <resume-signal>Type `approved` if the checklist passes, or describe the exact failing step and observed behavior.</resume-signal>
  <done>Human verification confirms the repo-side Sanity setup is usable and matches SAN-01 and SAN-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked docs→developer workstation | Setup instructions control how humans handle configuration and external Sanity settings |
| local app→Sanity auth/CORS settings | Embedded Studio depends on external origin allowlists outside the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | I | `.env.example` | mitigate | Document only public Sanity identifiers and explicitly exclude tokens or secrets from tracked env files. |
| T-01-08 | D | `README.md` setup instructions | mitigate | Include explicit CORS/authenticated-origin steps so embedded Studio access does not fail silently in local or deployed environments. |
| T-01-09 | R | human verification workflow | mitigate | Require a blocking checkpoint with concrete steps and expected results so the summary records whether `/studio` and schema behavior were actually tested. |
</threat_model>

<verification>
Run `npm run lint` after Task 1 and `npm run build` after Task 2. Do not close the plan until the blocking `/studio` verification checkpoint is answered.
</verification>

<success_criteria>
The Phase 1 foundation is documented end to end, required env keys are explicit, and a human has a repeatable process to validate embedded Studio access and schema behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sanity-foundation/01-03-SUMMARY.md`
</output>
