---
phase: 03-category-work-experience
plan: 2
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/work/[slug]/page.tsx
  - components/WorkGalleryGrid.tsx
  - sanity/lib/work.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-02
must_haves:
  truths:
    - "The public `/work/[slug]` route represents a category gallery, not a legacy project detail page."
    - "Known category slugs generate static params and unknown slugs fail closed."
    - "Visible gallery items are sourced from the Sanity helper layer."
  artifacts:
    - path: app/work/[slug]/page.tsx
      provides: Category gallery route and category identity presentation
    - path: components/WorkGalleryGrid.tsx
      provides: Reusable gallery rendering for visible category images
    - path: sanity/lib/work.ts
      provides: Any category lookup helper or shaping needed for the page route
  key_links:
    - from: app/work/[slug]/page.tsx
      to: sanity/lib/work.ts
      via: getWorkCategories and getGalleryItemsByCategorySlug
      pattern: getGalleryItemsByCategorySlug
---

<objective>
Repurpose `/work/[slug]` into the new category gallery route.

Purpose: Satisfy CAT-01 and CAT-02 by replacing the legacy detail-page behavior with category-owned gallery browsing.
Output: A category gallery page that clearly communicates the category identity and renders all visible gallery items assigned to that category.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-category-work-experience/03-RESEARCH.md
@.planning/phases/03-category-work-experience/03-SOURCE-AUDIT.md
@.planning/phases/03-category-work-experience/03-VALIDATION.md
@app/work/[slug]/page.tsx
@app/work/page.tsx
@components/ProjectCard.tsx
@sanity/lib/work.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the existing slug route from legacy project detail to category gallery</name>
  <files>app/work/[slug]/page.tsx, sanity/lib/work.ts</files>
  <behavior>
    - Test 1: `generateStaticParams()` is based on published category slugs, not legacy project slugs.
    - Test 2: The page renders category title and description clearly enough that visitors know what they are browsing.
    - Test 3: Unknown slugs return `notFound()` instead of partially rendering legacy detail assumptions.
  </behavior>
  <action>Replace the legacy detail-page implementation in `app/work/[slug]/page.tsx` with a category gallery page. Pull category identity from the published categories and use `getGalleryItemsByCategorySlug(slug)` for the visible item list. Keep the public route shape simple and category-first; do not introduce nested or temporary replacement URLs.</action>
  <verify>
    <automated>npm run lint -- 'app/work/[slug]/page.tsx' sanity/lib/work.ts && npm run build</automated>
  </verify>
  <done>`/work/[slug]` is now a category gallery route with clear category identity and safe slug handling.</done>
</task>

<task type="auto">
  <name>Task 2: Build the category gallery rendering component</name>
  <files>components/WorkGalleryGrid.tsx, app/work/[slug]/page.tsx</files>
  <behavior>
    - Test 1: Visible images for the selected category render in an editorial layout that works on desktop and mobile.
    - Test 2: The gallery component remains image-centric and does not reintroduce project-detail metadata blocks.
    - Test 3: The route can render with any number of visible items, including a single image.
  </behavior>
  <action>Create a gallery component for category pages that emphasizes imagery and category identity without restoring the old project-detail template. The route may include lightweight page-level navigation back to `/work`, but should not attempt the Phase 4 global nav repointing.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Category pages render visible images through a dedicated category gallery presentation instead of the old project-detail page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route params → category lookup | User-controlled slugs select which category page to render |
| Category gallery route → Sanity helper layer | Visible image reads come from remote CMS data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T | `app/work/[slug]/page.tsx` | mitigate | Match params only against known category slugs and return `notFound()` for unknown values. |
| T-03-04 | I | Category page presentation | mitigate | Render only the category-first data needed for browsing; do not leak or depend on stale legacy bridge fields. |
</threat_model>

<verification>
- `npm run lint -- 'app/work/[slug]/page.tsx' sanity/lib/work.ts`
- `npm run build`
</verification>

<success_criteria>
- `CAT-01` is materially in place: `/work/[slug]` shows visible images for the selected category.
- `CAT-02` is materially in place: visitors can clearly tell which category they are browsing.
- The legacy project-detail behavior is no longer the public meaning of `/work/[slug]`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-category-work-experience/03-category-work-experience-02-SUMMARY.md`
</output>
