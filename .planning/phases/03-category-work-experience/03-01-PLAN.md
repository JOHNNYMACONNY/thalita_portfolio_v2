---
phase: 03-category-work-experience
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/work/page.tsx
  - components/WorkCategoryCard.tsx
  - app/types/index.ts
  - sanity/lib/work.ts
  - next.config.ts
autonomous: true
requirements:
  - WORK-01
must_haves:
  truths:
    - "The `/work` landing page is category-first, not project-first."
    - "The page is powered by published Sanity category documents rather than markdown or legacy bridge projections."
    - "The three seeded categories remain the editorial source of truth."
  artifacts:
    - path: app/work/page.tsx
      provides: Server-rendered Work landing route
    - path: components/WorkCategoryCard.tsx
      provides: Editorial category-card presentation for the new Work landing page
    - path: sanity/lib/work.ts
      provides: Any small helper additions needed to support category-first route rendering
    - path: next.config.ts
      provides: Minimum remote image support for Sanity-hosted category cover images if runtime requires it
  key_links:
    - from: app/work/page.tsx
      to: sanity/lib/work.ts
      via: getWorkCategories
      pattern: getWorkCategories
    - from: components/WorkCategoryCard.tsx
      to: app/work/[slug]/page.tsx
      via: Link href
      pattern: /work/
---

<objective>
Create the dedicated `/work` landing page that presents the three Sanity-backed Work categories as editorial browse cards.

Purpose: Satisfy WORK-01 and establish the public entry point for the category-based Work IA.
Output: A new `/work` route and category card component wired to the published category documents in Sanity.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-category-work-experience/03-RESEARCH.md
@.planning/phases/03-category-work-experience/03-SOURCE-AUDIT.md
@.planning/phases/03-category-work-experience/03-VALIDATION.md
@app/page.tsx
@components/PortfolioGrid.tsx
@components/ProjectCard.tsx
@app/types/index.ts
@sanity/lib/work.ts
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a category-first `/work` landing route and presentation component</name>
  <files>app/work/page.tsx, components/WorkCategoryCard.tsx, app/types/index.ts</files>
  <behavior>
    - Test 1: `/work` renders from Sanity category data rather than legacy `Project` projections.
    - Test 2: Each category card shows cover image, title, description, and a link to its category page.
    - Test 3: The page preserves the existing editorial tone rather than introducing a generic gallery grid.
  </behavior>
  <action>Create `app/work/page.tsx` as a server route that calls `getWorkCategories()` and renders the three published categories through a dedicated `WorkCategoryCard` component. Reuse the existing visual language where possible, but do not reuse `PortfolioGrid` or `ProjectCard` if their project-detail assumptions make the category experience awkward. Keep copy and hierarchy browse-oriented, not project-detail-oriented.</action>
  <verify>
    <automated>npm run lint -- app/work/page.tsx components/WorkCategoryCard.tsx app/types/index.ts</automated>
  </verify>
  <done>The repo has a real `/work` page that renders the three Sanity-backed categories as intentional editorial cards.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure category cover media can render on the landing page</name>
  <files>sanity/lib/work.ts, next.config.ts</files>
  <behavior>
    - Test 1: Category cover image URLs resolve cleanly through the existing Sanity helper layer.
    - Test 2: The Next.js image configuration does not block Sanity-hosted category media.
    - Test 3: The implementation only makes the minimum media-enabling change needed for Phase 3.
  </behavior>
  <action>If the current route implementation requires it, make the narrowest possible change to support Sanity-hosted category cover images in Next.js. Keep the change scoped to enabling Phase 3 route rendering; do not broaden it into Phase 5 cleanup.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The `/work` landing page can render category cover images from Sanity in a production build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js server → Sanity Content Lake | Server-rendered category reads consume remote CMS data |
| Next.js image runtime → remote image host | Cover images cross from trusted app code into remote media rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `/work` data flow | mitigate | Keep the landing page on `getWorkCategories()` and fail closed on malformed category records instead of guessing missing fields. |
| T-03-02 | D | Category cover media | mitigate | Verify build/runtime image support explicitly so `/work` does not ship broken media placeholders. |
</threat_model>

<verification>
- `npm run lint -- app/work/page.tsx components/WorkCategoryCard.tsx app/types/index.ts`
- `npm run build`
</verification>

<success_criteria>
- `WORK-01` is materially in place: `/work` shows the three published category cards from Sanity.
- The landing page is category-first and no longer depends on legacy project cards.
- Category cover images render successfully in the app build.
</success_criteria>

<output>
After completion, create `.planning/phases/03-category-work-experience/03-category-work-experience-01-SUMMARY.md`
</output>
