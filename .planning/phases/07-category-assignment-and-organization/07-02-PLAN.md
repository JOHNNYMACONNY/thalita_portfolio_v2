---
phase: 07-category-assignment-and-organization
plan: 2
type: execute
wave: 1
depends_on:
  - 07-01-PLAN.md
files_modified:
  - sanity/components/PhotoOrganizerTool.tsx
autonomous: true
requirements:
  - ORG-02
  - ORG-03
must_haves:
  truths:
    - "Bulk organization must allow both reassignment and removal back to Unassigned."
    - "The editor needs quick ways to find photos that still need action."
  artifacts:
    - path: sanity/components/PhotoOrganizerTool.tsx
      provides: Bulk actions, search, and high-value filter views
---

<objective>
Add the bulk organization controls that make large batches manageable without opening each photo.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add bulk reassignment and remove-to-unassigned controls</name>
  <files>sanity/components/PhotoOrganizerTool.tsx</files>
  <action>Support bulk moves into category slots, reassignment to a different slot, and removal back to `Unassigned` with clear safety copy.</action>
  <verify>
    <automated>npm run lint -- sanity/components/PhotoOrganizerTool.tsx</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add search and filter helpers for quick cleanup</name>
  <files>sanity/components/PhotoOrganizerTool.tsx</files>
  <action>Add fast filter/search affordances so the editor can focus on unsorted, hidden, or featured photos without paging through the entire library.</action>
  <verify>
    <automated>npm run lint -- sanity/components/PhotoOrganizerTool.tsx</automated>
  </verify>
</task>

</tasks>
