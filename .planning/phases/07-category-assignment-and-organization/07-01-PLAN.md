---
phase: 07-category-assignment-and-organization
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/components/PhotoOrganizerTool.tsx
autonomous: true
requirements:
  - ORG-01
must_haves:
  truths:
    - "Category slots must stay visually obvious while the editor is sorting photos."
    - "The organizer should remain photo-first rather than collapsing back into form-first editing."
  artifacts:
    - path: sanity/components/PhotoOrganizerTool.tsx
      provides: Multi-bucket organizer surface with visible destination slots
---

<objective>
Turn the starter organizer into a visual workspace that shows `Unassigned` and all three category slots together.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Expand the organizer layout into visible sorting buckets</name>
  <files>sanity/components/PhotoOrganizerTool.tsx</files>
  <action>Replace the one-list organizer flow with a workspace that keeps `Unassigned` and all category slots visible together so category destinations are always obvious.</action>
  <verify>
    <automated>npm run lint -- sanity/components/PhotoOrganizerTool.tsx</automated>
  </verify>
</task>

</tasks>
