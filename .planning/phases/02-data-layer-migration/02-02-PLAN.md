---
phase: 02-data-layer-migration
plan: 2
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - package.json
  - sanity/migrations/work-category-map.ts
  - sanity/migrations/work-import.ts
  - sanity/migrations/out/.gitkeep
autonomous: true
requirements:
  - MIG-01
user_setup:
  - service: sanity
    why: Dataset import requires authenticated CLI access to the target project and dataset
    env_vars:
      - name: NEXT_PUBLIC_SANITY_PROJECT_ID
        source: Existing `.env.local` / Sanity project settings
      - name: NEXT_PUBLIC_SANITY_DATASET
        source: Existing `.env.local` / Sanity dataset name
    dashboard_config:
      - task: Ensure the operator is authenticated for CLI dataset import
        location: Run `npx sanity login` if local CLI auth is not already present
must_haves:
  truths:
    - "Every legacy cover image and gallery image becomes exactly one import record."
    - "Category ambiguity is explicit and blocks import until mapped."
    - "The import is deterministic and rerunnable without silent duplication."
  artifacts:
    - path: sanity/migrations/work-category-map.ts
      provides: Explicit legacy-to-canonical category and slug mapping rules
    - path: sanity/migrations/work-import.ts
      provides: Manifest generation, NDJSON emission, deterministic IDs, and import-ready output
    - path: package.json
      provides: A repeatable migration command
  key_links:
    - from: sanity/migrations/work-import.ts
      to: content/projects
      via: gray-matter parsing and manifest expansion
      pattern: content/projects
    - from: sanity/migrations/work-import.ts
      to: sanity/migrations/work-category-map.ts
      via: explicit category resolution
      pattern: work-category-map
    - from: sanity/migrations/work-import.ts
      to: sanity/migrations/out
      via: manifest and NDJSON output
      pattern: out
---

<objective>
Create the one-off import path that transforms legacy markdown Work content into Sanity-native categories and gallery items.

Purpose: Satisfy the migration half of Phase 2 with a deterministic, rerunnable import flow that preserves images and category assignments.
Output: Checked-in mapping rules, import script, and a package command that emits manifest and NDJSON artifacts for Sanity dataset import.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer-migration/02-RESEARCH.md
@.planning/phases/02-data-layer-migration/02-SOURCE-AUDIT.md
@.planning/phases/01-sanity-foundation/01-02-SUMMARY.md
@content/projects/editorial-noir.md
@content/projects/mariko-daisy-ep-2025.md
@content/projects/personal-elevation.md
@content/projects/urban-decay.md
@sanity/schemaTypes/documents/category.ts
@sanity/schemaTypes/documents/galleryItem.ts

<interfaces>
From sanity/schemaTypes/documents/category.ts:
```typescript
const categorySlotOrderById: Record<string, number> = {
  "work-category-1": 1,
  "work-category-2": 2,
  "work-category-3": 3,
};
```

Legacy source shape from content/projects/*.md:
```yaml
title: Editorial Noir
slug: editorial-noir
categories:
  - Editorial
  - Lookbook
coverImage: /images/gallery-1.jpg
gallery:
  - src: /images/gallery-2.jpg
    alt: Close up of fabric texture
```

From sanity/schemaTypes/documents/galleryItem.ts:
```typescript
{ name: "image", type: "image" }
{ name: "alt", type: "string" }
{ name: "category", type: "reference", to: [{ type: "category" }] }
{ name: "isVisible", type: "boolean" }
{ name: "showOnHomePage", type: "boolean" }
{ name: "homePageOrder", type: "number" }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode explicit category reconciliation and manifest derivation rules</name>
  <files>sanity/migrations/work-category-map.ts, sanity/migrations/work-import.ts</files>
  <behavior>
    - Test 1: A legacy record with an approved mapping resolves to one canonical category ID.
    - Test 2: A record with ambiguous or unmapped categories fails the run instead of falling back to the first category.
    - Test 3: Each source cover image and gallery image expands to its own manifest row with deterministic identifiers.
  </behavior>
  <action>Create an explicit mapping module that resolves legacy category names and known ambiguous slugs to the fixed Phase 1 category IDs. In `work-import.ts`, parse `content/projects/*.md`, normalize image sources under the project root, and emit an intermediate manifest that expands one gallery item per cover/gallery image, per `C-02` and `C-04`. Abort on unmapped categories, missing image files, duplicate deterministic IDs, or unsupported source shapes.</action>
  <verify>
    <automated>npx tsx sanity/migrations/work-import.ts --dry-run --strict</automated>
  </verify>
  <done>The import script can derive a complete manifest from legacy markdown, and any unresolved category or file issue stops the run with actionable errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit import artifacts and wire a repeatable migration command</name>
  <files>sanity/migrations/work-import.ts, package.json, sanity/migrations/out/.gitkeep</files>
  <behavior>
    - Test 1: The script writes a reviewed manifest and NDJSON payload with deterministic document IDs.
    - Test 2: The generated NDJSON references local assets in the format expected by Sanity dataset import.
    - Test 3: The repo exposes a stable CLI command for rerunning the migration path.
  </behavior>
  <action>Finish `work-import.ts` so it writes `manifest.json` and `import.ndjson` into `sanity/migrations/out/`, preserving required alt text, visibility flags, home-page flags/order, single category refs, and temporary legacy bridge metadata. Add a package script for dry-run/output generation and keep the output directory tracked with `.gitkeep`. Use deterministic IDs plus rerun-safe import instructions (`--replace`/`--missing`) rather than ad hoc random IDs, per research `R-04`.</action>
  <verify>
    <automated>npm run work:migrate -- --dry-run --strict</automated>
  </verify>
  <done>The repo can regenerate import artifacts on demand, and those artifacts are ready for `npx sanity dataset import` without manual document editing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local markdown/assets → migration script | Untrusted legacy content is converted into importable CMS documents |
| Migration script → Sanity dataset import | Bulk content write crosses into the canonical CMS dataset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `sanity/migrations/work-category-map.ts` | mitigate | Keep category resolution in a checked-in explicit mapping table; reject fallback heuristics and abort on unmapped values. |
| T-02-05 | T | `sanity/migrations/work-import.ts` | mitigate | Normalize and validate every referenced image path under the repo root before emitting `_sanityAsset` references to prevent bad or out-of-tree paths. |
| T-02-06 | R | Import reruns | mitigate | Generate deterministic `_id` values and stable manifest rows so reruns can be audited and reconciled instead of producing untraceable duplicates. |
</threat_model>

<verification>
- `npx tsx sanity/migrations/work-import.ts --dry-run --strict`
- `npm run work:migrate -- --dry-run --strict`
</verification>

<success_criteria>
- `MIG-01` has a concrete import implementation, not a manual placeholder flow.
- Every legacy image maps to one import record with one canonical category reference.
- The migration path blocks on ambiguity instead of guessing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer-migration/02-data-layer-migration-02-SUMMARY.md`
</output>
