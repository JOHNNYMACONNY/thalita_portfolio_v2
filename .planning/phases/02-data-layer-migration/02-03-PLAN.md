---
phase: 02-data-layer-migration
plan: 3
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - README.md
  - sanity/migrations/work-audit.ts
  - .planning/phases/02-data-layer-migration/02-IMPORT-RECEIPT.md
  - .planning/phases/02-data-layer-migration/02-MIGRATION-AUDIT.md
autonomous: true
requirements:
  - SAN-03
  - MIG-01
must_haves:
  truths:
    - "Imported category and gallery-item counts can be compared against the legacy source and the active Sanity helpers."
    - "Any missing images, unmapped categories, or incomplete imported records are surfaced in a durable audit artifact."
    - "The migration runbook documents how to rerun import plus verification without touching non-Work content."
  artifacts:
    - path: sanity/migrations/work-audit.ts
      provides: Automated completeness and parity audit for imported Work data
    - path: .planning/phases/02-data-layer-migration/02-IMPORT-RECEIPT.md
      provides: Durable record of the executed dataset import that the audit can cross-check
    - path: .planning/phases/02-data-layer-migration/02-MIGRATION-AUDIT.md
      provides: Checked-in audit results and manual cleanup list
    - path: README.md
      provides: Operator runbook for import plus audit commands
  key_links:
    - from: sanity/migrations/work-audit.ts
      to: sanity/lib/work.ts
      via: helper-level parity checks
      pattern: sanity/lib/work
    - from: sanity/migrations/work-audit.ts
      to: sanity/migrations/work-category-map.ts
      via: expected source expansion and category reconciliation
      pattern: work-category-map
    - from: sanity/migrations/work-audit.ts
      to: .planning/phases/02-data-layer-migration/02-IMPORT-RECEIPT.md
      via: receipt consistency check against the imported dataset
      pattern: 02-IMPORT-RECEIPT.md
    - from: sanity/migrations/work-audit.ts
      to: .planning/phases/02-data-layer-migration/02-MIGRATION-AUDIT.md
      via: generated report output
      pattern: 02-MIGRATION-AUDIT.md
---

<objective>
Verify that the imported Sanity dataset is complete and document any remaining editorial cleanup before Phase 3 route work starts.

Purpose: Close the migration loop with explicit completeness checks so Phase 3 can build on trusted Sanity data instead of assumptions.
Output: An automated audit script, a checked-in migration audit report, and README runbook updates for the import/verify flow.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer-migration/02-RESEARCH.md
@.planning/phases/02-data-layer-migration/02-SOURCE-AUDIT.md
@.planning/phases/02-data-layer-migration/02-IMPORT-RECEIPT.md
@.planning/phases/01-sanity-foundation/01-03-SUMMARY.md
@content/projects/editorial-noir.md
@content/projects/mariko-daisy-ep-2025.md
@content/projects/personal-elevation.md
@content/projects/urban-decay.md
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a Sanity-versus-legacy completeness audit</name>
  <files>sanity/migrations/work-audit.ts</files>
  <behavior>
    - Test 1: The audit computes expected image/category counts from the same legacy source expansion rules used by the migration.
    - Test 2: The audit checks the imported Sanity dataset for missing records, missing required image/category fields, and mismatched counts.
    - Test 3: The audit can verify helper-level SAN-03 parity by exercising the Sanity fetch wrappers used by the app.
    - Test 4: The audit cross-checks the import receipt against the queried dataset so the recorded import target matches the audited content.
  </behavior>
  <action>Create `work-audit.ts` to derive expected records from the checked-in legacy content, compare those expectations against the imported Sanity dataset plus the active `sanity/lib/work.ts` helper outputs, and fail on count mismatches, missing assets, missing category refs, or missing legacy bridge fields required for the overlap period. Cross-check the dataset and document counts against `02-IMPORT-RECEIPT.md` so the audit can detect a wrong-target or partial import run. Keep the audit focused on Work only, per `C-01`, and make ambiguity or missing imports first-class failures per `C-04`, `C-05`, and `C-07`.</action>
  <verify>
    <automated>npx tsx sanity/migrations/work-audit.ts --report .planning/phases/02-data-layer-migration/02-MIGRATION-AUDIT.md --strict</automated>
  </verify>
  <done>The repo has an executable audit that proves whether imported Sanity data matches legacy expectations and helper-level access.</done>
</task>

<task type="auto">
  <name>Task 2: Record the migration audit and operator runbook</name>
  <files>.planning/phases/02-data-layer-migration/02-IMPORT-RECEIPT.md, .planning/phases/02-data-layer-migration/02-MIGRATION-AUDIT.md, README.md</files>
  <action>Run the audit after import, commit the generated `02-MIGRATION-AUDIT.md` report alongside `02-IMPORT-RECEIPT.md`, and update `README.md` with the Phase 2 operator flow: generate artifacts, get mapping approval, import dataset content, rerun the audit, and record any manual editorial cleanup. The audit report must explicitly call out unresolved category mappings, missing source assets, missing imported records, receipt-versus-dataset mismatches, and any manual fixes still required before Phase 3 begins, per `C-05` and `C-07`.</action>
  <verify>
    <automated>rg -n \"Phase 2 migration|work:migrate|work:import|work-audit|manual cleanup|02-MIGRATION-AUDIT|02-IMPORT-RECEIPT\" README.md .planning/phases/02-data-layer-migration/02-MIGRATION-AUDIT.md .planning/phases/02-data-layer-migration/02-IMPORT-RECEIPT.md</automated>
  </verify>
  <done>The import receipt and migration report are checked in, the runbook is documented, and remaining manual cleanup items are explicit instead of tribal knowledge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit script → Sanity dataset | Verification code reads the canonical CMS data and decides whether it is trustworthy |
| Audit report → future phase execution | Phase 3 depends on the audit output to avoid building on incomplete content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | R | `.planning/phases/02-data-layer-migration/02-MIGRATION-AUDIT.md` | mitigate | Generate the report from the audit script and check it in so completeness claims are traceable to concrete command output. |
| T-02-08 | I | `README.md` runbook | mitigate | Document only the required public env values and CLI-auth flow; do not add write tokens or private credentials to tracked files. |
| T-02-09 | D | Phase 3 handoff | mitigate | Treat count mismatches, missing assets, and missing category refs as blocking audit failures so downstream route work cannot proceed on incomplete data. |
</threat_model>

<verification>
- `npx tsx sanity/migrations/work-audit.ts --report .planning/phases/02-data-layer-migration/02-MIGRATION-AUDIT.md --strict`
- `rg -n "Phase 2 migration|work:migrate|work-audit|manual cleanup|02-MIGRATION-AUDIT" README.md .planning/phases/02-data-layer-migration/02-MIGRATION-AUDIT.md`
</verification>

<success_criteria>
- `MIG-01` is closed with a concrete completeness report, not just an import script.
- `SAN-03` is rechecked through the app-facing Sanity helper layer.
- Manual editorial cleanup, if any, is explicitly documented before Phase 3 starts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer-migration/02-data-layer-migration-03-SUMMARY.md`
</output>
