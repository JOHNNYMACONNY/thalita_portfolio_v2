---
phase: 08-placement-and-publishing-controls
plan: 2
type: execute
wave: 1
depends_on:
  - 08-01-PLAN.md
files_modified:
  - sanity/components/PhotoOrganizerTool.tsx
  - sanity/schemaTypes/documents/galleryItem.ts
  - sanity/lib/queries.ts
  - sanity/lib/work.ts
  - app/types/index.ts
autonomous: true
requirements:
  - PLC-02
must_haves:
  truths:
    - "Category order must be independent from homepage order."
    - "The visual organizer should own ordering while numeric fields stay implementation detail."
  artifacts:
    - path: sanity/schemaTypes/documents/galleryItem.ts
      provides: Schema support for category ordering
    - path: sanity/lib/queries.ts
      provides: Public category pages that respect category order
---

<objective>
Add visual ordering controls for both category placement and homepage placement, then connect them to the public data layer.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add category and homepage reorder controls</name>
  <files>sanity/components/PhotoOrganizerTool.tsx</files>
  <action>Provide visual reorder controls in the organizer so the editor can change category and homepage sequence without editing number fields manually.</action>
  <verify>
    <automated>npm run lint -- sanity/components/PhotoOrganizerTool.tsx</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update schema and public queries for category order</name>
  <files>sanity/schemaTypes/documents/galleryItem.ts, sanity/lib/queries.ts, sanity/lib/work.ts, app/types/index.ts</files>
  <action>Add a category-specific order field and update the Work query layer so category pages honor the organizer's visual sequence.</action>
  <verify>
    <automated>npm run lint -- sanity/schemaTypes/documents/galleryItem.ts sanity/lib/queries.ts sanity/lib/work.ts app/types/index.ts</automated>
  </verify>
</task>

</tasks>
