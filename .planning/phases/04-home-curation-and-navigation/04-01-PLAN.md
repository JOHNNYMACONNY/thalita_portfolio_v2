---
phase: 04-home-curation-and-navigation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/HomeGalleryGrid.tsx
  - sanity/lib/work.ts
autonomous: true
requirements:
  - HOME-01
  - HOME-02
must_haves:
  truths:
    - "The homepage Work section is driven by curated `galleryItem` records, not legacy `Project` bridge data."
    - "Only items flagged for home display appear on the homepage."
    - "The visible order of home items comes from Sanity `homePageOrder`."
  artifacts:
    - path: app/page.tsx
      provides: Homepage route wired to curated home-gallery data
    - path: components/HomeGalleryGrid.tsx
      provides: Image-led curated home gallery presentation without project-detail assumptions
    - path: sanity/lib/work.ts
      provides: Any narrow helper hardening needed to preserve curated ordering and fail closed on malformed home items
  key_links:
    - from: app/page.tsx
      to: sanity/lib/work.ts
      via: getHomeGalleryItems
      pattern: getHomeGalleryItems
    - from: components/HomeGalleryGrid.tsx
      to: app/work/page.tsx
      via: Link href
      pattern: /work
---

<objective>
Replace the homepage Work section with a curated Sanity-backed home gallery.

Purpose: Satisfy `HOME-01` and establish the real editorial home curation path for the new Work architecture.
Output: The homepage renders only published `galleryItem` documents marked for home display, in the order defined by Sanity.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-home-curation-and-navigation/04-RESEARCH.md
@.planning/phases/04-home-curation-and-navigation/04-VALIDATION.md
@app/page.tsx
@components/PortfolioGrid.tsx
@app/types/index.ts
@sanity/lib/work.ts
@sanity/lib/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the legacy homepage grid data source with curated home gallery items</name>
  <files>app/page.tsx, sanity/lib/work.ts</files>
  <behavior>
    - Test 1: The homepage calls `getHomeGalleryItems()` instead of `getLegacyProjectsFromSanity()`.
    - Test 2: Only `showOnHomePage == true` items can appear in the home Work section.
    - Test 3: Returned item order is preserved and not re-sorted in the page layer.
  </behavior>
  <action>Rewire the home route so its Work section is powered by the canonical home-curation helper. Keep the implementation fail-closed for malformed or empty records, and do not add fallback logic that broadens the section to all visible gallery items when no home-curated items exist.</action>
  <verify>
    <automated>npm run lint -- app/page.tsx sanity/lib/work.ts</automated>
  </verify>
  <done>The homepage Work data flow is Sanity-curated and ordered according to `homePageOrder`.</done>
</task>

<task type="auto">
  <name>Task 2: Build a home-specific gallery presentation that preserves the site's editorial tone</name>
  <files>app/page.tsx, components/HomeGalleryGrid.tsx</files>
  <behavior>
    - Test 1: The home gallery is image-led and does not revive legacy project-card or project-slug assumptions.
    - Test 2: If the curated set is empty, the section still feels intentional and continues to funnel visitors toward `/work`.
    - Test 3: Any click behavior aligns with the new Work IA rather than nonexistent project-detail destinations.
  </behavior>
  <action>Create a dedicated home gallery component instead of stretching the legacy `PortfolioGrid` into the new content model. Preserve the site's existing editorial rhythm, but keep the section aligned with category/gallery browsing and avoid linking tiles to legacy project-detail routes.</action>
  <verify>
    <automated>npm run lint -- app/page.tsx components/HomeGalleryGrid.tsx && npm run build</automated>
  </verify>
  <done>The homepage has a curated, image-led Work section that matches the new information architecture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Homepage route -> Sanity helper layer | Server-rendered home curation reads remote CMS data |
| Homepage gallery UI -> public Work routes | Visitor clicks from curated content into the new browse flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | Homepage data flow | mitigate | Keep the home route on `getHomeGalleryItems()` and reject malformed records instead of inferring project data. |
| T-04-02 | D | Home curation ordering and empty state | mitigate | Preserve helper ordering exactly and render a deliberate empty curated state instead of collapsing or broadening the section. |
</threat_model>

<verification>
- `npm run lint -- app/page.tsx components/HomeGalleryGrid.tsx sanity/lib/work.ts`
- `npm run build`
</verification>

<success_criteria>
- `HOME-01` is materially in place: the homepage shows only curated home gallery items from Sanity.
- `HOME-02` is prepared correctly in code: homepage order follows helper output derived from `homePageOrder`.
- The home route no longer depends on legacy `Project` bridge rendering for Work discovery.
</success_criteria>

<output>
After completion, create `.planning/phases/04-home-curation-and-navigation/04-home-curation-and-navigation-01-SUMMARY.md`
</output>
