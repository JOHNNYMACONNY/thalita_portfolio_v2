---
phase: 04-home-curation-and-navigation
plan: 2
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - components/Header.tsx
  - components/Hero.tsx
  - components/NavigationOverlay.tsx
  - components/OverlayMenu.tsx
  - lib/api.ts
autonomous: true
requirements:
  - WORK-02
must_haves:
  truths:
    - "Primary Work discovery routes point to `/work`, not `/#portfolio`."
    - "Desktop and mobile navigation stay visually consistent while changing only destination semantics."
    - "Shared nav configuration does not drift from visible navigation behavior."
  artifacts:
    - path: components/Header.tsx
      provides: Desktop Work navigation pointed at `/work`
    - path: components/Hero.tsx
      provides: Hero CTA pointed at `/work`
    - path: components/NavigationOverlay.tsx
      provides: Mobile overlay Work navigation pointed at `/work`
    - path: lib/api.ts
      provides: Shared site nav configuration aligned with the same route target
  key_links:
    - from: components/Header.tsx
      to: app/work/page.tsx
      via: Link href
      pattern: /work
    - from: components/Hero.tsx
      to: app/work/page.tsx
      via: Link href
      pattern: /work
    - from: components/NavigationOverlay.tsx
      to: app/work/page.tsx
      via: Link href
      pattern: /work
---

<objective>
Repoint shared Work navigation from the legacy homepage anchor to the new `/work` route.

Purpose: Satisfy `WORK-02` so the site's primary discovery paths consistently lead into the new category-based Work experience.
Output: Header, hero CTA, mobile overlay navigation, and shared nav config all route to `/work`.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-home-curation-and-navigation/04-RESEARCH.md
@.planning/phases/04-home-curation-and-navigation/04-VALIDATION.md
@components/Header.tsx
@components/Hero.tsx
@components/NavigationOverlay.tsx
@components/OverlayMenu.tsx
@lib/api.ts
@app/work/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all active shared Work entry paths to `/work`</name>
  <files>components/Header.tsx, components/Hero.tsx, components/NavigationOverlay.tsx</files>
  <behavior>
    - Test 1: Desktop header Work link routes to `/work`.
    - Test 2: The hero primary CTA routes to `/work`.
    - Test 3: The active mobile overlay Work link routes to `/work`.
  </behavior>
  <action>Update the shared navigation surfaces visitors actively use so they lead into the new category-first Work entry point. Preserve the existing editorial layout, animation, and typography; this plan changes route targets, not the site's visual direction.</action>
  <verify>
    <automated>npm run lint -- components/Header.tsx components/Hero.tsx components/NavigationOverlay.tsx</automated>
  </verify>
  <done>Every active Work discovery path in site chrome opens `/work` instead of a homepage anchor.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminate leftover legacy Work anchors from shared nav sources</name>
  <files>lib/api.ts, components/OverlayMenu.tsx</files>
  <behavior>
    - Test 1: Shared nav config matches the new `/work` destination.
    - Test 2: Any reachable fallback menu implementation no longer references `#portfolio`.
    - Test 3: The repo no longer contains stale shared-navigation Work anchors after the sweep.
  </behavior>
  <action>Align the hardcoded nav configuration and any residual shared menu implementation with the same `/work` destination so future refactors cannot accidentally reintroduce legacy Work anchors. If `OverlayMenu.tsx` is unused, keep the touch minimal but leave it semantically correct.</action>
  <verify>
    <automated>npm run lint -- lib/api.ts components/OverlayMenu.tsx && rg -n '/#portfolio|#portfolio' app components lib</automated>
  </verify>
  <done>The shared navigation layer is internally consistent and free of stale Work anchors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared navigation UI -> public routes | Visitors rely on nav targets to discover the approved Work IA |
| Config helpers -> visible chrome | Hardcoded nav config can silently drift from rendered navigation behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | T | Shared navigation links | mitigate | Update every active Work entry path to `/work` in one sweep so route semantics stay consistent. |
| T-04-04 | R | Nav config drift | mitigate | Sweep shared config and fallback menu code for stale `#portfolio` anchors after the main link changes. |
</threat_model>

<verification>
- `npm run lint -- components/Header.tsx components/Hero.tsx components/NavigationOverlay.tsx lib/api.ts components/OverlayMenu.tsx`
- `rg -n '/#portfolio|#portfolio' app components lib`
</verification>

<success_criteria>
- `WORK-02` is materially in place: header, overlay navigation, and hero CTA all route to `/work`.
- Shared navigation config no longer encodes the old homepage-anchor discovery path.
- The repo is free of stale shared-navigation `#portfolio` links in `app`, `components`, and `lib`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-home-curation-and-navigation/04-home-curation-and-navigation-02-SUMMARY.md`
</output>
