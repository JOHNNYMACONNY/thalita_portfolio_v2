---
phase: 04-home-curation-and-navigation
plan: 3
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - app/page.tsx
  - components/HomeGalleryGrid.tsx
  - components/Header.tsx
  - components/Hero.tsx
  - components/NavigationOverlay.tsx
  - .planning/phases/04-home-curation-and-navigation/04-UAT.md
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - WORK-02
must_haves:
  truths:
    - "The homepage curated section remains intentional on desktop and mobile."
    - "Visitors can reach `/work` cleanly from every primary discovery path."
    - "Sanity editor reordering is proven against the live homepage, not assumed from code."
  artifacts:
    - path: app/page.tsx
      provides: Final homepage spacing and curated-state hardening
    - path: components/HomeGalleryGrid.tsx
      provides: Final responsive curated gallery treatment
    - path: .planning/phases/04-home-curation-and-navigation/04-UAT.md
      provides: Manual verification script and results for route targets and home-page ordering
  key_links:
    - from: .planning/phases/04-home-curation-and-navigation/04-UAT.md
      to: app/page.tsx
      via: manual homepage verification
      pattern: /
    - from: .planning/phases/04-home-curation-and-navigation/04-UAT.md
      to: app/work/page.tsx
      via: manual navigation verification
      pattern: /work
---

<objective>
Harden the homepage curation experience and close the phase with real editorial verification.

Purpose: Finish `HOME-01`, `HOME-02`, and `WORK-02` with responsive polish and a recorded Studio-driven reorder proof.
Output: Final homepage and nav presentation plus a checked-in UAT record covering route targets, mobile/desktop behavior, and home-page ordering.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-home-curation-and-navigation/04-RESEARCH.md
@.planning/phases/04-home-curation-and-navigation/04-VALIDATION.md
@app/page.tsx
@components/HomeGalleryGrid.tsx
@components/Header.tsx
@components/Hero.tsx
@components/NavigationOverlay.tsx
@app/work/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune homepage curated-state behavior across breakpoints</name>
  <files>app/page.tsx, components/HomeGalleryGrid.tsx, components/Header.tsx, components/Hero.tsx, components/NavigationOverlay.tsx</files>
  <behavior>
    - Test 1: The homepage Work section remains composed and legible on desktop and mobile.
    - Test 2: A zero-curated state still feels deliberate and continues to route visitors to `/work`.
    - Test 3: Navigation and hero changes still feel consistent with the site's existing editorial language.
  </behavior>
  <action>Refine spacing, hierarchy, and any empty curated-state handling so the home route feels finished across screen sizes. Keep the touch narrowly focused on polish and resilience; do not expand this phase into a broader redesign or legacy cleanup.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>The home curation experience holds up across breakpoints and empty curated sets feel intentional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live home-page curation order and Work navigation end to end</name>
  <files>.planning/phases/04-home-curation-and-navigation/04-UAT.md</files>
  <action>Record a simple UAT script and results covering: homepage curated-item rendering, desktop and mobile Work entry links landing on `/work`, and at least one Studio reorder of curated home items proving the homepage follows `homePageOrder`. The checked-in record must name the gallery items or titles used for the reorder test, the before/after order observed on the homepage, and whether the change appeared without code changes or workaround refreshes.</action>
  <what-built>The final homepage curated gallery plus `/work`-based discovery flow.</what-built>
  <how-to-verify>
    1. Open `/` on desktop and confirm only curated home items are visible in the Work section.
    2. Click the header Work link and hero CTA; confirm both land on `/work`.
    3. Open the mobile navigation overlay and confirm its Work link also lands on `/work`.
    4. In `/studio`, change `homePageOrder` across at least two visible curated items and publish.
    5. Refresh `/` and confirm the visible order matches the new published order.
    6. Record the tested item titles, the observed order before and after, and any anomalies in `04-UAT.md`.
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <resume-signal>Type `approved` to continue, or describe the exact issue that failed.</resume-signal>
  <done>Phase 4 has a checked-in verification record proving `/work` discovery and live home-page reorder behavior end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser homepage -> responsive layouts | Curated homepage presentation must stay intentional across screen sizes |
| Manual Studio workflow -> published homepage | Editorial reordering must propagate into the public site correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | D | Homepage curated presentation | mitigate | Tune responsive spacing and empty-state behavior explicitly rather than assuming the Phase 1-3 layouts transfer cleanly. |
| T-04-06 | R | Editorial reorder verification | mitigate | Require a checked-in UAT artifact that records a real Studio reorder and the resulting homepage order. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
- Manual UAT recorded in `.planning/phases/04-home-curation-and-navigation/04-UAT.md`
</verification>

<success_criteria>
- `HOME-01` is visually hardened across breakpoints and zero-curated states.
- `WORK-02` is confirmed in real browser flows on desktop and mobile.
- `HOME-02` is proven with a real published reorder in Studio that changes homepage item order as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/04-home-curation-and-navigation/04-home-curation-and-navigation-03-SUMMARY.md`
</output>
