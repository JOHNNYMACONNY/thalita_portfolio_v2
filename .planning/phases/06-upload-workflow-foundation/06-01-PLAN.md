---
phase: 06-upload-workflow-foundation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity.config.ts
  - sanity/structure.ts
  - sanity/schemaTypes/documents/galleryItem.ts
  - sanity/lib/work.ts
  - sanity/lib/queries.ts
  - README.md
autonomous: true
requirements:
  - UPL-03
  - EDT-01
must_haves:
  truths:
    - "Upload-first only works if uncategorized gallery items are treated as a safe editor state rather than invalid content."
    - "The public site must continue to hide uncategorized items from `/`, `/work`, and `/work/[slug]`."
    - "Studio should speak in editor tasks and plain labels, not expose raw document taxonomy as the main workflow."
  artifacts:
    - path: sanity/schemaTypes/documents/galleryItem.ts
      provides: Upload-safe gallery item schema with an intentional unassigned state
    - path: sanity/structure.ts
      provides: Task-oriented Studio navigation for photo intake
    - path: sanity/lib/work.ts
      provides: Public helper layer that fails closed when category assignment is missing
    - path: sanity.config.ts
      provides: Studio customization hooks aligned to the simplified upload workflow
  key_links:
    - from: app/page.tsx
      to: sanity/lib/work.ts
      via: getHomeGalleryItems
      pattern: getHomeGalleryItems
    - from: app/work/page.tsx
      to: sanity/lib/work.ts
      via: getWorkCategories
      pattern: getWorkCategories
    - from: app/work/[slug]/page.tsx
      to: sanity/lib/work.ts
      via: getGalleryItemsByCategorySlug
      pattern: getGalleryItemsByCategorySlug
---

<objective>
Define the editor-facing upload model and Studio foundation that make upload-first photo intake possible.

Purpose: Establish `Unassigned` as a safe content state and reshape the Studio around editor tasks instead of raw document types.
Output: The schema, structure, and public helper layer all support upload-first intake without leaking uncategorized items into the live site.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/v1.1-simple-photo-management.md
@.planning/phases/06-upload-workflow-foundation/06-RESEARCH.md
@.planning/phases/06-upload-workflow-foundation/06-VALIDATION.md
@sanity.config.ts
@sanity/structure.ts
@sanity/schemaTypes/documents/galleryItem.ts
@sanity/lib/work.ts
@sanity/lib/queries.ts
@app/page.tsx
@app/work/page.tsx
@app/work/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redefine gallery-item intake so upload can happen before category assignment</name>
  <files>sanity/schemaTypes/documents/galleryItem.ts, sanity/lib/work.ts, sanity/lib/queries.ts</files>
  <behavior>
    - Test 1: `galleryItem` documents can exist without immediate category assignment.
    - Test 2: Public helper/query code still excludes uncategorized items from all live route surfaces.
    - Test 3: Validation/copy explain that uncategorized content is an intentional intake state, not an error.
  </behavior>
  <action>Refactor the gallery-item schema and supporting helper/query assumptions so upload-first intake is safe. Preserve the existing public category/gallery behavior by failing closed whenever category placement is missing.</action>
  <verify>
    <automated>npm run lint -- sanity/schemaTypes/documents/galleryItem.ts sanity/lib/work.ts sanity/lib/queries.ts && npm run build</automated>
  </verify>
  <done>Gallery items can be uploaded before categorization, and the public site still ignores unassigned intake items.</done>
</task>

<task type="auto">
  <name>Task 2: Rework Studio navigation and guidance around editor tasks instead of raw document lists</name>
  <files>sanity.config.ts, sanity/structure.ts, README.md</files>
  <behavior>
    - Test 1: Studio exposes a clearer upload/intake entry point than the current flat `Gallery Items` list.
    - Test 2: Labels and descriptions use plain language that makes sense to a non-technical editor.
    - Test 3: The structure still preserves access to category management and all uploaded items.
  </behavior>
  <action>Use the existing Studio customization points to shift the editor workflow toward upload and intake tasks. Add plain-language labels/help text and keep the structure consistent with the three-category model already in production.</action>
  <verify>
    <automated>npm run lint -- sanity.config.ts sanity/structure.ts README.md && npm run build</automated>
  </verify>
  <done>Studio exposes an upload-friendly intake model with clearer labels and task-oriented navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio intake state -> public site | Unassigned items must not surface publicly before categorization |
| Schema flexibility -> editor confidence | Allowing uncategorized documents must not make the workflow feel broken or ambiguous |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I | Public gallery routes | mitigate | Keep helper/query layer fail-closed for missing category assignment. |
| T-06-02 | U | Studio editor flow | mitigate | Replace raw document-list emphasis with task-oriented structure and plain-language guidance. |
</threat_model>

<verification>
- `npm run lint -- sanity.config.ts sanity/structure.ts sanity/schemaTypes/documents/galleryItem.ts sanity/lib/work.ts sanity/lib/queries.ts README.md`
- `npm run build`
</verification>

<success_criteria>
- `UPL-03` is materially advanced: newly uploaded photos can exist safely before categorization.
- `EDT-01` is materially advanced: Studio navigation and guidance are more understandable for a non-technical editor.
- The public site continues to ignore uncategorized items.
</success_criteria>

<output>
After completion, create `.planning/phases/06-upload-workflow-foundation/06-upload-workflow-foundation-01-SUMMARY.md`
</output>
