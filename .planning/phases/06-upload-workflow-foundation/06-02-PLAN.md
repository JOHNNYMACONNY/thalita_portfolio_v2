---
phase: 06-upload-workflow-foundation
plan: 2
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - sanity.config.ts
  - sanity/structure.ts
  - sanity/schemaTypes/documents/galleryItem.ts
  - sanity/components/**
  - app/studio/**
autonomous: true
requirements:
  - UPL-01
  - UPL-02
  - UPL-03
  - EDT-01
must_haves:
  truths:
    - "The upload flow should feel photo-first and batch-friendly, not like repetitive document creation."
    - "Unassigned should be visible and recoverable so editors can upload now and sort later."
    - "Phase 6 should prefer focused Studio customization over building a separate admin product."
  artifacts:
    - path: sanity/components/
      provides: Upload-facing editor components or field polish for a simpler intake flow
    - path: sanity/structure.ts
      provides: A visible intake surface for newly uploaded photos
    - path: app/studio/[[...tool]]/page.tsx
      provides: Embedded Studio route still serving the customized upload workflow
  key_links:
    - from: app/studio/[[...tool]]/page.tsx
      to: sanity.config.ts
      via: NextStudio config
      pattern: NextStudio
---

<objective>
Build the batch upload and intake surface that lets editors add many photos quickly and understand what happens next.

Purpose: Deliver the first concrete editor-facing upload improvements once the data model and Studio foundations are ready.
Output: The Studio exposes a clear, batch-friendly upload path with thumbnails/progress feedback and a visible `Unassigned` destination for new images.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/v1.1-simple-photo-management.md
@.planning/phases/06-upload-workflow-foundation/06-RESEARCH.md
@.planning/phases/06-upload-workflow-foundation/06-VALIDATION.md
@.planning/phases/06-upload-workflow-foundation/06-01-PLAN.md
@sanity.config.ts
@sanity/structure.ts
@sanity/schemaTypes/documents/galleryItem.ts
@app/studio/[[...tool]]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the main batch upload intake surface inside Studio</name>
  <files>sanity.config.ts, sanity/structure.ts, sanity/components/**, app/studio/**</files>
  <behavior>
    - Test 1: The editor has a clearly discoverable place to upload many photos in one session.
    - Test 2: The upload experience shows enough thumbnail/progress feedback that the editor knows the upload worked.
    - Test 3: The batch upload entry point still fits naturally inside the embedded `/studio` experience.
  </behavior>
  <action>Implement the upload-facing Studio affordance using the minimal customization that produces a clearly better editor experience. Prioritize obvious entry points, thumbnails, progress clarity, and copy that tells the editor what happens after upload.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>Editors can clearly discover and use the new batch upload entry point in Studio.</done>
</task>

<task type="auto">
  <name>Task 2: Make newly uploaded images visibly land in an `Unassigned` intake workflow</name>
  <files>sanity/structure.ts, sanity/schemaTypes/documents/galleryItem.ts, sanity/components/**</files>
  <behavior>
    - Test 1: Newly uploaded images are visible in a dedicated intake/unassigned area.
    - Test 2: The interface tells the editor these images are not live on the public site until organized later.
    - Test 3: The workflow reduces pressure to categorize every image immediately.
  </behavior>
  <action>Wire the upload result into the `Unassigned` intake state so the editor can upload first and organize later. The UI should make the holding state obvious and safe, rather than feeling like incomplete broken content.</action>
  <verify>
    <automated>npm run lint -- sanity/structure.ts sanity/schemaTypes/documents/galleryItem.ts && npm run build</automated>
  </verify>
  <done>New uploads land in a visible, safe intake queue that the editor can return to later.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upload UI -> document creation | Batch upload must create coherent intake-state content instead of half-configured editor records |
| Upload feedback -> user trust | Editors need clear proof of success/failure to avoid duplicate uploads or confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | U | Upload entry point | mitigate | Make the upload surface obvious and batch-friendly with clear progress/thumbnail feedback. |
| T-06-04 | I | Unassigned intake queue | mitigate | Explicitly label newly uploaded items as not yet assigned / not yet live so the workflow feels safe. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- `UPL-01` is materially advanced: the editor can batch upload multiple photos in one flow.
- `UPL-02` is materially advanced: the upload flow gives understandable feedback.
- `UPL-03` is reinforced by a visible unassigned landing state.
</success_criteria>

<output>
After completion, create `.planning/phases/06-upload-workflow-foundation/06-upload-workflow-foundation-02-SUMMARY.md`
</output>
