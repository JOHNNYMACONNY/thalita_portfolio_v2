---
phase: 05-legacy-cms-removal
plan: 3
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - next.config.ts
  - netlify.toml
  - README.md
  - content/projects
  - .planning/phases/05-legacy-cms-removal/05-UAT.md
autonomous: false
requirements:
  - MIG-02
  - MIG-03
must_haves:
  truths:
    - "Image and deployment config should reflect the active Sanity-backed Work experience, not legacy CMS assumptions."
    - "Leftover markdown Work content should not continue to imply an active editorial source after cleanup."
    - "Phase 05 closes only after route and CMS regression are manually checked."
  artifacts:
    - path: next.config.ts
      provides: Final image/routing config aligned with the cleaned Sanity-backed app
    - path: netlify.toml
      provides: Deployment config reviewed against the post-Decap app surface
    - path: .planning/phases/05-legacy-cms-removal/05-UAT.md
      provides: Manual regression record for routes and CMS entry paths after cleanup
  key_links:
    - from: .planning/phases/05-legacy-cms-removal/05-UAT.md
      to: app/work/page.tsx
      via: manual route regression
      pattern: /work
    - from: .planning/phases/05-legacy-cms-removal/05-UAT.md
      to: app/studio/[[...tool]]/page.tsx
      via: manual CMS regression
      pattern: /studio
---

<objective>
Finalize image/deployment cleanup, remove leftover markdown Work assumptions, and close the cleanup phase with regression verification.

Purpose: Finish `MIG-02` and `MIG-03` by leaving only the active Sanity-backed Work stack and validating that the cleaned app still behaves correctly.
Output: Finalized config/docs/content cleanup plus a checked-in UAT record covering `/`, `/work`, category pages, `/studio`, and the removal of the old `/admin` surface.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-legacy-cms-removal/05-RESEARCH.md
@.planning/phases/05-legacy-cms-removal/05-VALIDATION.md
@next.config.ts
@netlify.toml
@README.md
@content/projects
@app/page.tsx
@app/work/page.tsx
@app/work/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize image/deployment cleanup and remove leftover markdown Work assumptions</name>
  <files>next.config.ts, netlify.toml, README.md, content/projects</files>
  <behavior>
    - Test 1: Image config supports the active Sanity-backed experience cleanly and no longer retains clearly obsolete legacy Work assumptions.
    - Test 2: Deployment config is still coherent after the admin/legacy route cleanup.
    - Test 3: Leftover markdown Work content or docs no longer implies an active editorial source for the current Work experience.
  </behavior>
  <action>Review and tighten the remaining image/deployment/content cleanup surface based on what is still truly active after Plans 01 and 02. Remove only assumptions that are now clearly obsolete; avoid speculative config churn that is not justified by the active app behavior.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <done>The active app/config/content surface reflects the Sanity-backed Work system cleanly and without obsolete legacy Work assumptions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify route and CMS regression after cleanup</name>
  <files>.planning/phases/05-legacy-cms-removal/05-UAT.md</files>
  <action>Record a simple UAT script and results covering: `/` loading successfully, `/work` and at least one category page rendering correctly, `/studio` still loading as the active CMS entry path, and the old `/admin` surface no longer serving the legacy Decap UI. The checked-in record must note whether any route unexpectedly redirected, 404ed, or retained old CMS/admin behavior after cleanup.</action>
  <what-built>The cleaned post-Decap, Sanity-only Work stack and its final deployment/image/runtime configuration.</what-built>
  <how-to-verify>
    1. Open `/` and confirm the homepage still renders normally.
    2. Open `/work` and at least one category page such as `/work/editorial`; confirm category browsing still works.
    3. Open `/studio` and confirm the embedded Sanity Studio still loads.
    4. Visit `/admin` and confirm the old Decap surface is gone or no longer serves the legacy CMS UI.
    5. Record any unexpected redirects, 404s, or regressions in `05-UAT.md`.
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <resume-signal>Type `approved` to continue, or describe the exact issue that failed.</resume-signal>
  <done>Phase 05 has a checked-in regression record proving the Sanity-backed app still works after legacy CMS cleanup and the old admin surface is no longer active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Image/deployment config -> active runtime | Final config cleanup must not break the surviving Sanity-backed experience |
| Browser routing -> CMS entry points | Cleanup must preserve `/studio` while removing the old `/admin` behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | D | Final config cleanup | mitigate | Pair config/content cleanup with a full build so obsolete assumptions are removed without breaking the active app. |
| T-05-06 | R | Post-cleanup regression | mitigate | Require a checked-in UAT artifact that verifies the surviving routes and the removal of the old admin surface. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
- Manual UAT recorded in `.planning/phases/05-legacy-cms-removal/05-UAT.md`
</verification>

<success_criteria>
- `MIG-02` is complete: legacy route/CMS/admin surface is removed without leaving broken navigation paths.
- `MIG-03` is complete: image and deployment config cleanly support the Sanity-backed Work experience.
- The cleaned app is regression-verified across homepage, Work routes, Studio, and the removed old admin path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-legacy-cms-removal/05-legacy-cms-removal-03-SUMMARY.md`
</output>
