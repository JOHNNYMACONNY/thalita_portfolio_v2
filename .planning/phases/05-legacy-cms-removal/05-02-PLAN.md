---
phase: 05-legacy-cms-removal
plan: 2
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - public/admin/index.html
  - public/admin/config.yml
  - public/admin/cms.js
  - public/admin/admin.css
  - public/admin/admin-preview.css
  - app/layout.tsx
  - next.config.ts
  - package.json
  - README.md
autonomous: true
requirements:
  - MIG-02
must_haves:
  truths:
    - "Decap removal must include its runtime glue, not just the static admin folder."
    - "Sanity Studio at `/studio` remains the active CMS path after cleanup."
    - "The repo should no longer document or script Decap as a supported workflow once this plan lands."
  artifacts:
    - path: app/layout.tsx
      provides: Root layout without obsolete Netlify Identity widget loading
    - path: next.config.ts
      provides: Routing config without the `/admin` Decap rewrite
    - path: package.json
      provides: Script surface aligned with the active CMS stack
    - path: README.md
      provides: Project docs describing Sanity Studio instead of Decap for Work editing
  key_links:
    - from: app/layout.tsx
      to: app/studio/[[...tool]]/page.tsx
      via: active CMS shell
      pattern: /studio
    - from: next.config.ts
      to: public/admin/index.html
      via: rewrite removal
      pattern: /admin
---

<objective>
Remove Decap CMS assets and the obsolete deployment/runtime glue that supported them.

Purpose: Satisfy the CMS-infrastructure portion of `MIG-02` after the Sanity-backed flow is verified.
Output: Decap admin assets, the `/admin` rewrite, Netlify Identity integration, old scripts, and stale docs are removed while `/studio` remains the active CMS entry point.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-legacy-cms-removal/05-RESEARCH.md
@.planning/phases/05-legacy-cms-removal/05-VALIDATION.md
@app/layout.tsx
@next.config.ts
@package.json
@README.md
@public/admin/index.html
@public/admin/config.yml
@public/admin/cms.js
@public/admin/admin.css
@public/admin/admin-preview.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Decap admin assets and their runtime/deployment glue</name>
  <files>public/admin/index.html, public/admin/config.yml, public/admin/cms.js, public/admin/admin.css, public/admin/admin-preview.css, app/layout.tsx, next.config.ts, package.json</files>
  <behavior>
    - Test 1: The static Decap admin asset set is removed from the repo.
    - Test 2: The root layout no longer loads the Netlify Identity widget.
    - Test 3: The app config no longer rewrites `/admin` into the removed Decap entrypoint.
  </behavior>
  <action>Delete the Decap admin files and remove the runtime/deployment glue that existed only to support them, including the identity widget, rewrite, and obsolete package script. Keep the cleanup coherent so no dead config points at removed files afterward.</action>
  <verify>
    <automated>npm run lint && rg -n 'admin|decap|netlify-identity|identity-widget' app public next.config.ts package.json</automated>
  </verify>
  <done>The repo no longer ships Decap assets or the runtime/deployment glue that supported them.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite project documentation around Sanity Studio as the only active Work CMS path</name>
  <files>README.md</files>
  <behavior>
    - Test 1: README no longer describes `public/admin` as an active project structure element.
    - Test 2: The docs describe `/studio` as the active editorial path for Work content.
    - Test 3: Documentation changes stay focused on the CMS/workflow cleanup rather than broad content rewriting.
  </behavior>
  <action>Update the README so the documented editorial workflow matches the live repo after Decap removal. Preserve useful Sanity setup guidance and remove stale CMS references that would now mislead a future maintainer.</action>
  <verify>
    <automated>npm run lint -- README.md</automated>
  </verify>
  <done>The repo documentation tells one coherent story: Sanity Studio is the active Work CMS path and Decap is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deployment/runtime config -> removed assets | Dead rewrites or widget scripts can leave broken or confusing runtime behavior |
| Project docs -> future maintainers | Stale CMS documentation can misdirect future setup and support work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | Decap infrastructure removal | mitigate | Remove static assets and all runtime/deployment glue in the same plan so the repo cannot reference deleted admin files. |
| T-05-04 | R | Documentation drift | mitigate | Update README in the same slice so the repo documents Sanity Studio as the only active Work CMS workflow. |
</threat_model>

<verification>
- `npm run lint`
- `rg -n 'admin|decap|netlify-identity|identity-widget' app public next.config.ts package.json README.md`
</verification>

<success_criteria>
- `MIG-02` is materially in place: Decap assets, scripts, rewrites, and identity integration are removed.
- `/studio` remains the only active CMS entry path described by the repo.
- No dead Decap/admin references remain in active runtime/config/docs surface.
</success_criteria>

<output>
After completion, create `.planning/phases/05-legacy-cms-removal/05-legacy-cms-removal-02-SUMMARY.md`
</output>
