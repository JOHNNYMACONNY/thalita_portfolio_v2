---
phase: 05-legacy-cms-removal
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/portfolio/page.tsx
  - app/types/index.ts
  - sanity/lib/work.ts
  - sanity/lib/queries.ts
  - sanity/schemaTypes/documents/galleryItem.ts
  - README.md
autonomous: true
requirements:
  - MIG-02
must_haves:
  truths:
    - "The public Work experience remains `/work` plus category pages; cleanup must not break those routes."
    - "Legacy project-detail bridge helpers are removed only where they are obsolete, not where active category/gallery code still depends on shared helpers."
    - "The hardcoded `app/portfolio/page.tsx` route is obsolete once the Sanity-backed Work flow is verified."
  artifacts:
    - path: sanity/lib/work.ts
      provides: Work helper layer without legacy project bridge exports
    - path: sanity/lib/queries.ts
      provides: Query surface trimmed to active category/gallery/home usage
    - path: app/types/index.ts
      provides: Type layer aligned with the active Sanity-backed Work contracts
    - path: app/portfolio/page.tsx
      provides: Removed obsolete standalone portfolio route
  key_links:
    - from: app/work/page.tsx
      to: sanity/lib/work.ts
      via: getWorkCategories
      pattern: getWorkCategories
    - from: app/work/[slug]/page.tsx
      to: sanity/lib/work.ts
      via: getGalleryItemsByCategorySlug
      pattern: getGalleryItemsByCategorySlug
---

<objective>
Remove obsolete Work routes and runtime bridge code that only existed for the old project-centric portfolio implementation.

Purpose: Satisfy the Work-route portion of `MIG-02` while preserving the current category-first public experience.
Output: The obsolete `app/portfolio` route and legacy project bridge helper/query/type surface are removed without destabilizing `/work` or `/work/[slug]`.
</objective>

<execution_context>
@/Users/bobbyinthelobby/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobbyinthelobby/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-legacy-cms-removal/05-RESEARCH.md
@.planning/phases/05-legacy-cms-removal/05-VALIDATION.md
@app/portfolio/page.tsx
@app/work/page.tsx
@app/work/[slug]/page.tsx
@app/types/index.ts
@sanity/lib/work.ts
@sanity/lib/queries.ts
@sanity/schemaTypes/documents/galleryItem.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the obsolete standalone portfolio route and any dead presentation dependencies</name>
  <files>app/portfolio/page.tsx, README.md</files>
  <behavior>
    - Test 1: The hardcoded `/portfolio` route is removed from the app codebase.
    - Test 2: No remaining documentation presents that route as an active Work experience.
    - Test 3: The cleanup does not touch the approved `/work` and `/work/[slug]` route surface.
  </behavior>
  <action>Delete the obsolete standalone portfolio route and update any nearby documentation references that still imply it is part of the active Work experience. Keep the change tightly scoped to old Work-route cleanup rather than general documentation rewriting.</action>
  <verify>
    <automated>npm run lint -- app app/work README.md</automated>
  </verify>
  <done>The obsolete hardcoded portfolio route is gone and the repo no longer treats it as active Work UI.</done>
</task>

<task type="auto">
  <name>Task 2: Remove legacy project-bridge helpers, queries, and schema/type surface no longer needed at runtime</name>
  <files>app/types/index.ts, sanity/lib/work.ts, sanity/lib/queries.ts, sanity/schemaTypes/documents/galleryItem.ts</files>
  <behavior>
    - Test 1: No active app route imports or depends on legacy project bridge helpers after cleanup.
    - Test 2: The type and query layers align with the active category/gallery/home Sanity model only.
    - Test 3: The cleanup preserves any migration audit files under `.planning/` or `sanity/migrations/out/` unless they are directly harmful to runtime behavior.
  </behavior>
  <action>Remove the temporary legacy-project bridge exports and supporting query/type/schema surface that were kept only for the overlap period. Preserve the active Work helpers and avoid deleting audit/history artifacts that do not participate in runtime behavior.</action>
  <verify>
    <automated>npm run lint -- app/types/index.ts sanity/lib/work.ts sanity/lib/queries.ts sanity/schemaTypes/documents/galleryItem.ts && npm run build</automated>
  </verify>
  <done>The active runtime no longer carries legacy project-bridge helper/query/type/schema surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Active Work routes -> helper layer | Cleanup must not remove code still required by `/work` and category pages |
| Runtime code -> historical migration artifacts | Audit/history files may remain useful even when runtime bridge code is removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | Work route cleanup | mitigate | Keep `/work` and `/work/[slug]` grounded on active category/gallery helpers while removing only obsolete route and bridge code. |
| T-05-02 | D | Legacy bridge removal | mitigate | Verify no active imports depend on the removed legacy helper/query/type surface before closing the plan. |
</threat_model>

<verification>
- `npm run lint -- app app/work README.md app/types/index.ts sanity/lib/work.ts sanity/lib/queries.ts sanity/schemaTypes/documents/galleryItem.ts`
- `npm run build`
</verification>

<success_criteria>
- `MIG-02` is materially advanced: obsolete standalone Work route code and runtime bridge logic are removed.
- The active `/work` and category-gallery flow remain intact.
- The app/runtime no longer depends on legacy project-bridge helper/query/type/schema surface.
</success_criteria>

<output>
After completion, create `.planning/phases/05-legacy-cms-removal/05-legacy-cms-removal-01-SUMMARY.md`
</output>
