# tech_context

## stack
- **Frontend**: Next.js (App Router preferred if applicable)
- **Styling**: Tailwind CSS
- **Deployment**: Netlify
- **CMS**: (Verify if Sanity, Contentful, or local markdown is used)

## architecture
- **Rendering**: Static Export / SSR / ISR (Verify in `package.json`)
- **State Management**: React Context / Zustand / Redux (Verify)

## conventions
- Use `kebab-case` for filenames.
- Use `PascalCase` for component names.
- Prefer `const` over `function` for components (or vice versa - set preference here).
